import engine;

/*class StandardUpdateFactory : IChunkUpdateFactory
{
  private{
    Camera cam;
    IChunkModelFactory modelFactory;
    Limiter modelLimiter;
  }

  this(Camera cam, IModelFactory modelFactory, )
  {
    this.cam = cam;
  }

  Updatable createChunkUpdate(ChunkGameObject chunkObject)
  {
    return new ChunkUpdateBehavior(cam, chunkObject, modelFactory, modelLimiter);
  }

  Updatable createChunkUpdate(ChunkGameObject[] chunk)
  {
    return new ChunkArrayUpdate(camera, chunkObject, modelFactory, modelLimiter);
  }
}*/